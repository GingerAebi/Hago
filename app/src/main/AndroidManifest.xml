<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jaebong.hago" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity
            android:name=".Activities.GenderAndAgeActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Activities.AtmosphereActivity"
            android:label="@string/title_activity_atmosphere" >
        </activity>

        <activity
            android:name=".Activities.AppearanceActivity"
            android:label="@string/title_activity_tall" >
        </activity>

        <activity
            android:name=".Activities.ResultListActivity"
            android:label="@string/title_activity_search" >
        </activity>
    </application>

</manifest>
